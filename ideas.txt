
AI Ideas:
- Split `SubtitleManager` into smaller pieces: subtitle parsing/cleaning, remote GitHub search, episode cache/download, and geometry calculation. It currently owns too many workflows, which makes performance bugs harder to isolate.
- Add cancellation tokens/generation checks to every background worker path: auto ruby, remote downloads, episode preparation, OCR, and Anki stroke sync. Some already have generation checks; make it consistent.
- Split `requirements.txt` into runtime and dev requirements later. The current file mixes app dependencies, benchmarks, Selenium experiments, and pytest.
- Make remote search cache files versioned or schema-tagged. That will make it safer to change search heuristics without accidentally using stale cached JSON.
- Consider a lightweight task queue abstraction for background work so throttling and shutdown behavior are handled in one place. This is mainly maintainability/shutdown safety, not a guaranteed performance improvement.
- SubtitleManager has some extracted helpers/modules, but it still owns parsing/cleaning, remote search, episode maps, downloads, ruby, and geometry.
- Add a small Anki preview/test area in Advanced Settings that shows effective tags and lookup form for a sample selected word plus sentence.
- Add a Performance tab "Copy diagnostics bundle" button that includes current subtitle file, episode, debug state, cache counts, and queue sizes.
- Add a local-config cleanup/migration action that removes stale keys from `config.local.json`.
- Add tests for multiple occurrences in one subtitle sentence, e.g. selecting `勉強` when only one occurrence is used as `勉強した`.
- Add a compact Anki failure details popup showing whether failure came from AnkiConnect, model fields, translation, or duplicate/media routing.
Subtitle difficulty heatmap, Review queue from episode (unknown words)

To Do:
when i go into tooltip and then fast back into the word the hover overlay is then behind the subtitle window and not visivle anymore. 


words like ２つ should also have a ruby futatsu

at one point apparently the lines showing in the subtitle window changed. It should be like this: ruby1
Line1
Line2
Ruby2
 if there is only one line in the subtitles it should be at line2 if there are two it should be line1 and line2. The ruby1 should alway be the rubies of line1 and same for 2. 
add name recognition with san and kun and so on. 
普通 is in the category ignored why?
when selecting kanji is the whitespace cleaned, when the user for example misselected whitespace before or after the kanji?
add a setting option so that when inisde subtitle window with the pointer the control window does not appear only when using the hotkeys to make it appear.
Counter of how many words i added this session or this day. 
option to remove the subtitle handle in phone mode
In Annotation tab: show counts for unknown, learning, young, mature, due today, suspended.
between m1 and m2 there can never be a conflict in shortcuts.
（ジャックの 雄たけびが 続く）was not hidden. Also （ヘリコプターの プロペラ 音が 続く）
持ってるな is correctly annotated in popup but not in the subttiel window why?持つ is in the word database 
when show controls when subtitle is hovered is turned off then the hotkey to show the subtitle window and the control window should work like this. IF none is shown show both, if only one of them is shown show the other one as well. if both are shown hide only the control window. 

Check if sound and image copy is valid.
when adding a card and then clicking outside the app and then wanting to add another card the hotkey to add does not work until i focus inside the app so clicking subtitle window or settings window.
Suddenly when adding new cards it is not automatically annotated even tough it is in the word database?
Only for certain words it worked: for example in this sentence: その 少年を 殺さず
監視 観察してほしい i added: 少年, 殺さず, 監視 and 観察 and only 観察 was marked right away after adding. 
我々 is not marked even tough it is in the word database?
Also when i have the popup open and select a word and to the ctrl+c hotkey then go to vscode and paste it there then go back to the popup and select the next word and want to copy it it doesnt work because it loses focus i guess?
in this sentence: いや
全てが 一瞬のことだったからな
全て and 一瞬 is in the word list but not marked? 
to click the hotkey to add another card again i need to click on the focus of the subtitle window when i clicked outside the app after adding the first word. Why? The hotkey shoudl still work because i select a text in the popup and then the hotkey does nothing until i clicked inside the app again.
still issues with annotations the popup is again right and different then the subtitle in this sentence たった１人 同じ 思いを
分かり 合える 人だったのに. 合える is not maked. Or 分かり 合える is wrongly marked in the popup? Are the new rules also for the popup?

- make an option to before adding, open a small anki style window, which shows the exact Front, Back, sentence, audio, image, tags. Useful for catching bad ruby or wrong dictionary form.
- make Better hover layers
Separate hover modes:
normal hover: ruby
Shift: dictionary
Ctrl: Anki/database status
Alt: sentence translation
- Add a fast forward option with 0.1 increments of speed and a hotkey to toggle it.
- Add profiles to have multiple users and saved settings which can be imported and exported.
- Make rubies copyable in popup. Need a design because sometimes plain text, dictionary text, and ruby text should copy differently.
- Automatically find OCR timebox, maybe with image recognition / machine learning.
- If OCR works well, jump/sync to the current video time when collecting Anki cards.
- Implement season/episode search in the settings search bar.
- Add HTML tag handling inside subtitle files as an optional mode.

Bugs:
- Remove remaining non-speaker modes is not correcting for deleting the line it is in (maybe implemented).
- When selecting the beginning of the sentence, the whole line to the right is selected.
- When switching from remote to local and then back to remote, the flag may not be set and it still searches in `subs`.
- The subtitle window can obstruct using the main window; currently mostly the minimize/titlebar buttons are affected.
- Changed auto ruby handling: check if words like `toutou` are still correct.
- Some windows like the search button window need to autosize.
- Add result/status if "Read Now" set-time did not work.

Dont do:
- If advanced window is not fully visible, have scroller and adjust size. Weird format, maybe later.
- Change seconds to milliseconds with certain skip buttons. Too clunky.
- Change subtitle width to fit current subtitle. Maybe flashes.

Added:
The subtitle dragging window should only be visible when phone mode is on. Currently it is also visible when not in phone mode. 
- Add optional toggles for automatic grammar marker tags (`する-Verb`, `い-Adj`, `な-Adj`) in case a deck does not want them.
- Add a word database which can be imported and exported to show the words you already know. Make a new tab with Annotation and one area is my word database where you can brows your wrods then beneath you can import or export words. These words will then be marked with (tbdl) if option is ticked on ("Mark words?"). Then next to it is the anki word database where you can configure target word and/or sentence fields to collect your known words from anki. Then another area with wanikani word database where an api token is needed (with button, open popup to insert the token). Then again for the anki area there is an option to Colorize subtitles based on known words, then there should be an option for specific level of words from anki for example >21 days (which can be modified) to not show the rubies for these words. Make an option to only show annotations on hover and highlight words on hover. Make the user choose the Anki decks and word and sentence fields. Also ask the user how to treat suspended Anki cards as dropdown(Normal, Mature, Young, Graduated, Learning Unknown). Also the styling should be optional so background underline overline outline thickness and so on choose color for mature young graduated learning unknown uncollected and the alpha and so on.
- Add an option to auto pause/trigger the spacebar to pause the background video as well, when hovering over the subtitle window with the mouse.
- Add a raster lock for the subtitle window that when it is near the middle of the screen it should snap to it also optional. (maybe later move the window with arrow keys?)
- Add a settings search/filter box for Advanced Settings tabs instead of scrolling or resizing the window.
- Replace remaining `print(...)` debug output with module loggers now that logging setup exists.
- when adding a new anki card it should also be added automatically to the word database
- Optional ruby detection.
- Add extra function for searching movies.
- Add filter when searching cached/saved remote searches.
- Advanced Settings Performance tab is available only while `DEBUGGING` is enabled with Ctrl+Shift+D; pressing it again hides the tab.
- Performance tab reports slider timing, subtitle render timing, episode switch timing, renderer cache/timing, auto-ruby stats, OCR duration, and queue sizes.
- Logging setup exists with runtime debug toggle and rotating log file support.
- ConfigManager now writes local/runtime changes to `config.local.json` over tracked `config.json` defaults.
- Anki custom tags can be set in Advanced Settings > Anki > Tags; no default `subtitleplayer` tag is added.
- Anki now adds grammar marker tags from sentence context: `する-Verb`, `い-Adj`, and `な-Adj`.
- Ctrl+A selects all text in popup.
- Hovering over kanji supports inline glossary and/or translation.
- X1 button text disappears if pressed again and reappears; same for button in control window.
- There is a space before kanji ruby segments in Anki where needed.
- Kanji ruby splitting can be enabled if needed.
- Duplicate English definition entries are deduped case-insensitively, e.g. `house` / `House`.
- Audio padding variable is available in Advanced Settings.
- X2/M3 mode has an Advanced Settings indicator/toggle.
- OCR box changes take effect at runtime.
- OCR hiding only affects windows that are actually inside OCR boxes.
- Adjusting offset also shifts the runtime position.
- Invalid string in control window no longer resets number unexpectedly.
- Advanced Settings no longer blocks the main settings window.
- Multiple OCR boxes across multiple screens are supported.
- Clear OCR area is not needed after region selection changes.
- Warning/wait flow exists if Anki is not connected.
- Spacebar and arrow key listener exists.
- Settings button can change defaults such as start time, offset, skip, phone mode, etc.
- Last used offset, phone mode, window geometry, and session state are remembered.
- Anki integration through AnkiConnect is available.
- Episode entry has a dropdown.
- Right-click subtitle overlay opens the copy popup.
- Startup/download splash/info exists.
- Button to search new anime exists.
- Anime name and season/episode can be shown in title/window context.
- Remote season subtitles can be downloaded and cached from a GitHub subtitle mirror.
- Kanji can be shown with ruby above.
- Control window has a button to show the settings window.
- Control window/time entry resizes for longer than 1 hour and movie-sized time strings.
- Phone mode default can be configured.

Fixed:
- Unhandled Tk callback/background-thread errors now show a message box with the log path instead of only printing or failing silently.
- Annotation matcher handles purpose forms like `俺を 殺しに` from `殺す` without re-enabling broad derived-noun matching like `動き` from `動く`.
- Exact annotation phrases can bridge subtitle spaces, so a database entry like `分かり合える` can win over a partial `分かる` match in `分かり 合える`.
- Spaced verb tails no longer make the whole spaced phrase known from the first verb alone.
- Kana/non-kanji exact phrases such as `だから` are covered by phrase annotation even when the tokenizer splits them.
- Popup Ctrl+C and popup add hotkeys are more robust after focus changes or a missed key-release event.
- Database-level checks confirm current entries for `普通`, `我々`, `全て`, `一瞬`, `少年`, `殺さず`, `監視`, and `観察して` match the active annotation provider.
- Pressing pause/play does not sync perfectly with the video. The app timer seems faster or starts earlier.
- Still double click to get entry focus. When i am outside of the advanced window in a different window then my app and then want to write inside the adv window it doesnt let me and the focus is still ont the extern window. 
- Selecting only a suru-capable stem now checks the subtitle sentence, e.g. selected `勉強` in `私は勉強した` becomes `勉強する` with `する-Verb`.
- Selecting a suru-capable noun used as a noun, e.g. `勉強は楽しい`, stays as `勉強` without `する-Verb`.
- Hovering over the titlebar/X area of the settings or advanced settings window now hides the phone-mode subtitle handle.
- Moving the subtitle window in phone mode also moves the small handle simultaneously.
- Removed/handled an extra unused window.
- Numpad hotkeys no longer jump while typing inside entries.
- Ruby display fixed for a Hitoribocchi line around 7 / 4:57.
- Default skip is saved.
- Advanced settings no longer switches through tabs unexpectedly.
- Advanced Settings Apply Now no longer resets changed offset/skip entries to defaults.
- Pressing skip from an unchanged focused time entry keeps the prior value.
- Newly added local SRT files can be found during runtime.
- Decreasing from the first episode no longer picks the last cached episode.
- Episode number updates only if an episode is found.
- Avoid creating season folder without finding season.
- Fixed wrong episode when turning off / changing episode state.
- Slider duration is updated correctly when offset or movie mode changes.
- New SRT selection updates the available SRT list.
- Changing to movie no longer keeps the max slider value from the previous episode.
- Subtitle window size recalculates after changing files.
- Selecting SRT after remote session is more reliable.
- GitHub SRT search was corrected.
- SRT button cannot be opened twice in a row while already open.
- If no `.srt` is found, the app does not reset to default slider position/state.
- Movie/non-Japanese subtitle width is handled better.
- Phone mode settings button issue fixed.
- Large/negative offset updates the slider correctly.
